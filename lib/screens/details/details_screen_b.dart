import 'package:flutter/material.dart';
import 'components/Blanco/body_b.dart';

class DetailsScreenBlanco extends StatelessWidget {
  const DetailsScreenBlanco({
    Key? key
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: BodyBlanco(),
    );
  }
}